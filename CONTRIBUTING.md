# 참여 규칙

## 브랜치

todo/번호 (예: todo/21, todo/53-54)

## 진행

1. git switch main && git pull
2. git switch -c todo/번호
3. TODO 채우고 동작 확인
4. git push -u origin todo/번호
5. PR 생성, 확인 후 스스로 merge
6. 브랜치 삭제, git switch main && git pull

## 안 고치는 파일

HTML 뼈대, 요소 id, style.css, api.js, common.js, study-page.js

## 커밋

동작 확인 후에만 push. 컴파일만 되는 상태로는 push 안 함.
